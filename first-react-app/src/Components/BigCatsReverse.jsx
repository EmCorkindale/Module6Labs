const BigCatsReverse = (props) => {
    return (
        <div className="ReverseCats">
            <button onClick={props.onReverse}>Reverse me!</button>
        </div>
    )
}
export default BigCatsReverse